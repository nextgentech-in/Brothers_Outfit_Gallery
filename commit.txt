fix: remove all fallback dummy products and fix cart local storage sync

- AccessoriesPage.jsx: removed fallbackProducts dummy dataset import so only real active Firestore products added by admin are displayed
- CartContext.jsx: automatically filter out legacy test/dummy cart items and continuously sync cart state (add, remove, clear) to localStorage so stale items never resurrect on reload
