fix: resolve Google sign-in auth/internal-error by configuring complete CSP and auth fallback

- vercel.json: added Google accounts/APIs/FirebaseApp domains to frame-src, script-src, connect-src, and form-action to prevent browser CSP blocking Google OAuth popup and redirect handshakes
- AuthContext.jsx: configured select_account prompt on GoogleAuthProvider and added graceful redirect fallback
