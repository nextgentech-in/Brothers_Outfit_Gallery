fix: remove whitespace in product cards by switching to object-fit cover

- ProductCard.css: changed object-fit from contain to cover with
  object-position top center so product images fill the card frame
  without any white gaps, while keeping the model face/top visible
